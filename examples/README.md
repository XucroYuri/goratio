# CSV 格式样例

`sample-import.csv` 仅用于演示 `date,gold_close,oil_close` 字段格式。数值为合成数据，不对应真实市场历史，也不得作为研究结果使用。

```bash
goratio update --source cn_public --import-csv examples/sample-import.csv
```

实际研究请导入你有权使用、且已核验口径的自有数据。
